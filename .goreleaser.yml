version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      # - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
    binary: gh-pat-rotate

archives:
  - name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - LICENSE*
      - README*
      - CHANGELOG*

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

homebrew:
  - repository:
      owner: tcurdt
      name: homebrew-tap
    homepage: "https://github.com/tcurdt/gh-pat-rotate"
    description: "Rotate GitHub PATs into environment secrets across multiple repositories"
    license: "Apache-2.0"
    directory: Formula
    install: |
      bin.install "gh-pat-rotate"
    test: |
      system "#{bin}/gh-pat-rotate", "--help"
